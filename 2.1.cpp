#include<iostream>
using namespace std;
int main() {
	double c, f; // Объявляем переменные
	cin >> f; // Вводим переменную - f
	c = (5.0 / 9) * (f - 32); // Присваиваем переменной - с полученное произведение
	cout << c; // Выводим переменную с
	return 0;
}
